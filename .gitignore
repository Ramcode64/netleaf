node_modules/
dist/
.env
.env.*
vercel.env
railway.env
*.log
.DS_Store
.next/

# NOTE: drizzle/ migration files ARE committed (source artifacts).
# Do not gitignore them — a fresh clone needs them to migrate the DB.
